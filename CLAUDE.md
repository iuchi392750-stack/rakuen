# rakuen

動画・音楽・ゲームなどの制作プロジェクト。

## 動画プロンプトを書く前に必ず読むこと

**動画生成のプロンプトを書く作業に入る前に、必ず
[`seedance/seedance25_prompt_guide.md`](seedance/seedance25_prompt_guide.md) を読むこと。**

ByteDance / Dreamina 公式の Seedance 2.5 プロンプトガイドを日本語で整理したもの。
Seedance に限らず、**あらゆる動画生成モデル(Hailuo / MiniMax H3、PixVerse、Veo など)の
プロンプトを書くときも、この作法に従う。**

とくに外しやすい要点:

- **設定値(アスペクト比・尺・解像度)はプロンプト本文に書かない。**UI 側で指定する
- 参照素材には必ず「**使うもの**」と「**使わないもの**」の両方を書く
- 参照素材は 1 体ずつ名前を付けて紐づける(`@Image 1 = <ドライバー>`)。画像内の文字ラベルには頼らない
- 出しうるものは**個数を固定する**(「出力には常に 1 台だけ映す」)
- 長尺は **1 ステージ = 1 変化**。各ステージに**フレームを止めて確認できる終了状態**を書く
- 音は括弧で区別する:`( )` 音楽 / `< >` 効果音 / `{ }` 台詞 / `【 】` 字幕
- 台詞の前に**言語を宣言する**(言語 + 地域・訛り + 話し方 + 話者 + `{台詞}`)
- 感情語は**見える手がかり 2〜4 個**に翻訳する

## ディレクトリ

| パス | 内容 |
|---|---|
| `seedance/` | 動画企画・プロンプト集・プロンプトガイド |
| `unity/` | Unity プロジェクト(ファミコン風レースゲーム) |
| `game/` | ブラウザ版の試作 |

## 制作環境の制約

このリポジトリで作業するセッションはクラウド上で動いており、ユーザーのローカル PC
(`C:\` ドライブ)には書き込めない。成果物はファイル送信と GitHub 経由で渡す。

動画生成は Higgsfield 連携経由でのみ実行できる。Hailuo(`hailuoai.video` /
`api.minimax.chat` / `preview.hailuoai.video`)は組織のポリシーで遮断されているため、
API・ブラウザのいずれからも到達できない。Hailuo で生成する場合はプロンプトを渡して
ユーザーが手元で実行する。
